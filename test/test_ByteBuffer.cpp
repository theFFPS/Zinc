#include <gtest/gtest.h>
#include <type/ByteBuffer.h>

TEST(ByteBufferTest, CreateByteBuffer) {
    zinc::ByteBuffer buffer;
    

    
    EXPECT_TRUE(true);
}

int main(int argc, char **argv) {
    ::testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
